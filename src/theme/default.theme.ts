import { createTheme } from "@mui/material/styles";

export const defaultTheme = createTheme({
  palette: {
    background: {
      default: "#FFF",
    },
    secondary: {
      main: "#4285F4",
      light: "#9B9B9B",
    },
  },
});
